'use strict';

var Joi = require('joi');

var QueryParamsSchema = Joi.object().keys({
  app_id: Joi.string().required(),
  limit: Joi.string(),
  offset: Joi.string()
}).required();

module.exports = QueryParamsSchema;