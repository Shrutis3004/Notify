'use strict';

var Joi = require('joi');

var SessionTimeSchema = Joi.object().keys({
  state: Joi.string().required(),
  active_time: Joi.number().integer().required()
}).required();

module.exports = SessionTimeSchema;